trigger:
  branches:
    include:
      - master

stages:

  - stage: Build_API
    jobs:
      - job: BuildDotNet
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - task: UseDotNet@2
            inputs:
              version: '10.0.x'
          - script: dotnet restore
            workingDirectory: 'ClinicAI.API'
          - script: dotnet build --configuration Release
            workingDirectory: 'ClinicAI.API'
          - script: dotnet test --no-build
            workingDirectory: 'ClinicAI.API'
          - script: dotnet publish -c Release -o $(Build.ArtifactStagingDirectory)/api
            workingDirectory: 'ClinicAI.API'
          - publish: $(Build.ArtifactStagingDirectory)/api
            artifact: api-drop

  - stage: Build_Angular
    jobs:
      - job: BuildAngular
        pool:
          vmImage: 'ubuntu-latest'
        steps:
          - task: NodeTool@0
            inputs:
              versionSpec: '20.x'
          - script: npm ci
            workingDirectory: 'clinic-ai-ui'
          - script: npm run build -- --configuration production
            workingDirectory: 'clinic-ai-ui'
          - publish: clinic-ai-ui/dist
            artifact: angular-drop

  - stage: Deploy
    dependsOn:
      - Build_API
      - Build_Angular
    jobs:
      - deployment: DeployAPI
        environment: 'production'
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureWebApp@1
                  inputs:
                    azureSubscription: 'clinic-azure-service-connection'
                    appName: 'clinic-ai-api'
                    package: $(Pipeline.Workspace)/api-drop
      - deployment: DeployAngular
        environment: 'production'
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureWebApp@1
                  inputs:
                    azureSubscription: 'clinic-azure-service-connection'
                    appName: 'clinic-ai-ui'
                    package: $(Pipeline.Workspace)/angular-drop/ui.zip